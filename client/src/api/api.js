const host = "http://localhost:8800/api";
const usersRoute = `${host}/users`;
const authRoute = `${host}/auth`;
const conversationRoute = `${host}/conversations`;
const messageRoute = `${host}/messages`;

const postRoute = `${host}/posts`;

export {
  host,
  usersRoute,
  authRoute,
  conversationRoute,
  messageRoute,
  postRoute,
};
